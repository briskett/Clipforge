# No server env vars required for the demo.
# Users supply OpenAI and ElevenLabs keys in the app wizard (stored in browser localStorage).

# Optional: GPU-accelerated FFmpeg encoding (auto|nvenc|qsv|amf|off). Default: auto (NVENC).
# FFMPEG_GPU=off
